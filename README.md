# qht202512
目前实现功能：
  基于纯python实现了网表解析，器件gds生成，对称的检测，简单floorplan：
  12/18：对称检测周中存在的局限：（symmetry.py）
          只检查了链接关系，但如果m1和m2宽度不同，虽然连成差分对但是不能按照对称布局/或者得加权对称
          交叉耦合和cascode没有写，而且应该加入一个对称组的传播（一个一个找复杂度o（n2）→ n）
        修改：
          比较net链接关系前加参数比较；增加了交叉耦合
          新建了一个集合，让一个器件只属于一个主要的对称关系
          输入对对称，那么负载大概率也对称，先找一个对称，然后根据net链接关系，检测下一组，同时将下一组连接的器件放到下一组需要检测的位置，传播检测会更快，同时这里的结果也写入上述的集合
          引脚验证的检查

  magical原有输出是一个正则简单文本，改成了json，对称之后的json里重复验证type、params的匹配
        新增：
        是否要求dummy管、guradring、强制方向等情况的检测接口，这里是留出一个手动写入的对称条件，但如何能够自动根据电路判定还没有思路
        
  坐标中心轴的判定：（axisdetector.py）
        原有判断是对称对的中点、通过vdd vss的中点等各个中间位置的中位数来确定
      修改：
        通过前边的集合，先分行，例如标注的差分对在行1，负载对在行2
        每一行建立一个中心轴x=0，对称对往两边推，推的距离根据宽度坐标和间距确定，y坐标根据分行坐标再上下累加
      新增：
        预留一个拆分的接口，把m1，m2拆成abba这样的格式，同时上下行距应该增加一个动态调整的算法，待完善
        
        
